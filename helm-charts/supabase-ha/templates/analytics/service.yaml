{{- if .Values.analytics.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-ha.analytics.fullname" . }}
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.analytics.service.type }}
  ports:
    - port: {{ .Values.analytics.service.port }}
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-ha.analytics.selectorLabels" . | nindent 4 }}
{{- end }}