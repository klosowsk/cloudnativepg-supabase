{{- if .Values.rest.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-ha.rest.fullname" . }}
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.rest.service.type }}
  ports:
    - port: {{ .Values.rest.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-ha.rest.selectorLabels" . | nindent 4 }}
{{- end }}