{{- if .Values.postgresql.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "supabase-ha.fullname" . }}-test-db
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
        - command:
            - /bin/sh
            - -c
            - |
              pg_isready -h $(DB_HOST) -p $(DB_PORT) -U $(DB_USER) || $(echo "\e[0;31mFailed to connect to the database." && exit 1)
              echo "Database is ready"
          env:
            - name: DB_HOST
              value: {{ include "supabase-ha.postgresHost" . | quote }}
            - name: DB_USER
              value: "postgres"
            - name: DB_PORT
              value: {{ include "supabase-ha.postgresPort" . | quote }}
          image: postgres:15-alpine
          imagePullPolicy: IfNotPresent
          name: test-db
      restartPolicy: Never
{{- end }}
