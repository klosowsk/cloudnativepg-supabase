{{- if .Values.meta.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "supabase-ha.fullname" . }}-test-meta
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
        - name: test-meta
          image: kdevup/curljq
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - |
              curl -sfo /dev/null \
                http://{{ include "supabase-ha.meta.fullname" . }}:{{ .Values.meta.service.port }}/health
              echo "Sevice {{ include "supabase-ha.meta.fullname" . }} is healthy."
      restartPolicy: Never
{{- end }}
