{{- if .Values.studio.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-ha.studio.fullname" . }}
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.studio.service.type }}
  ports:
    - port: {{ .Values.studio.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-ha.studio.selectorLabels" . | nindent 4 }}
{{- end }}