{{- if .Values.imgproxy.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-ha.imgproxy.fullname" . }}
  labels:
    {{- include "supabase-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.imgproxy.service.type }}
  ports:
    - port: {{ .Values.imgproxy.service.port }}
      targetPort: 5001
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-ha.imgproxy.selectorLabels" . | nindent 4 }}
{{- end }}